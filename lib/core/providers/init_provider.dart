import 'package:flutter_riverpod/flutter_riverpod.dart';

final appInitProvider = Provider<Future<void>>((ref) {
  throw UnimplementedError(
    'Initialize this provider in main.dart with the initFuture',
  );
});
